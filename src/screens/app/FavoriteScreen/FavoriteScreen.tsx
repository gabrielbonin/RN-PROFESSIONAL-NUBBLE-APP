import React from 'react';

import {Screen, Text} from '@components';
import {AppTabScreenProps} from '@routes';

export function FavoriteScreen() {
  return (
    <Screen>
      <Text preset="headingSmall">New Favorite Screen</Text>
    </Screen>
  );
}
